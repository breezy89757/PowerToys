<winuiex:WindowEx
    x:Class="MarkdownReader.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    MinWidth="480"
    MinHeight="320"
    mc:Ignorable="d">

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="TitleBar" Height="32">
             <TextBlock Text="Markdown Reader" VerticalAlignment="Center" Margin="12,0,0,0" Style="{StaticResource CaptionTextBlockStyle}"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <!-- Sidebar (TOC) -->
            <Border Grid.Column="0" Background="{ThemeResource SurfaceStrokeColorDefault}">
                <TextBlock Text="Table of Contents" Margin="10" />
            </Border>

            <!-- Main Content (WebView2) -->
            <controls:WebView2 x:Name="MarkdownWebView" Grid.Column="1" />
        </Grid>
    </Grid>
</winuiex:WindowEx>
